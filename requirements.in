# Source file for requirements.txt. Edit this, then run:
#   python -m piptools compile --output-file=requirements.txt requirements.in
# from the repo root. Commit both files. CI and Docker install from
# requirements.txt (or requirements-prod.txt for production).

Django==5.2.10
djangorestframework
djoser
django-cors-headers
django-extensions
python-dotenv
Pillow
stripe
requests
celery[redis]
django-celery-beat
redis
channels[daphne]
channels-redis
firebase-admin
